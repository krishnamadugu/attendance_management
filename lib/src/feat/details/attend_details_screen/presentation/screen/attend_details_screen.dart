import 'package:flutter/material.dart';

class AttendDetailsScreen extends StatelessWidget {
  const AttendDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
